---
layout: post
title: js系列7:Hono
date: 2024-03-07 04:30:00 +0800
categories: [js系列]
tags: [js系列]
---
Hono 是专门面向边缘运行时（Cloudflare Workers、Deno、Bun 等）的“小而强”框架，写起来很像 Express/Koa，但更贴近 Workers 的 Fetch API。

Deno 是一个现代的 JavaScript / TypeScript 运行时，可以把它理解为 Node.js 的“下一代改进版”。


Bun 是一个超快的 JavaScript 运行时 + 工具链，目标很直接：
👉 “比 Node.js 和 Deno 更快，同时更省事”


pnpm install -g wrangler
wrangler init my-worker
cd my-worker
pnpm install hono
wrangler dev


Wrangler 是 Cloudflare Workers 的官方 CLI（命令行工具）。
一句话理解：

Wrangler = 用来创建、开发、部署 Cloudflare Workers 的工具

wrangler dev
本地起一个开发服务器

行为尽量模拟 Cloudflare 边缘环境


wrangler deploy
几秒钟就能把代码部署到 全球 Cloudflare 节点

其它命令
npx wrangler d1 execute prompt-marketplace-db --local --file=scripts/clear_data.sql

wrangler d1 create prompts 会在cloudflare云上创建一个数据库